enum AppBarHeaders { home, aboutMe, projects, contact }

enum AppBarHeadersAxis { horizontal, vertical }

enum DeviceType {
  mobile,
  ipad,
  smallScreenLaptop,
  largeScreenDesktop,
  extraLargeTV
}

enum CollectionName {
  products,
  inActiveProducts,
  orders,
  users,
  userOrders,
  finishedOrders,
}
